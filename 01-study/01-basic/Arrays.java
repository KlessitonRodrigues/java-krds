import java.util.ArrayList;

public class Arrays {

    class NotaDeAvaliacao {
        double nota;
        double peso;
    }

    public static void main() {

        // Vector
        int[] numVector = { 1, 2, 3 }; // fixed length
        int[] numVector2 = new int[10]; // fixed length

        // ArrayList
        ArrayList<Integer> numArray = new ArrayList<>();
        numArray.get(0);
        numArray.remove(0);
        numArray.size();
    }
}

class notaDeAvaliacoes {
    double nota = 0;
    int peso = 0;
}
